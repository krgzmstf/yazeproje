# ──────────────────────────────────────────────────────────────
# YAZE Proje – Docker Compose Production Overrides
# Usage: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
# ──────────────────────────────────────────────────────────────
version: "3.9"

services:
  postgres:
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}
    restart: always

  redis:
    command: redis-server --requirepass ${REDIS_PASSWORD:-}
    restart: always

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      DEBUG: "false"
      ENVIRONMENT: production
      USE_SQLITE: "false"
      POSTGRES_HOST: postgres
      POSTGRES_USER: yaze_user
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}
      POSTGRES_DB: yaze_db
      REDIS_HOST: redis
    command: >
      uvicorn app.main:app
      --host 0.0.0.0
      --port 8000
      --workers 4
      --no-access-log
    volumes:
      - backend_uploads:/app/uploads
    restart: always

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_URL: https://yazeproje.com/api/v1
    container_name: yaze-frontend
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_API_URL: https://yazeproje.com/api/v1
    restart: always

  nginx:
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
      - certbot_webroot:/var/www/certbot:ro
    restart: always

  # ── Certbot (Let's Encrypt) ────────────────────────────────
  certbot:
    image: certbot/certbot:latest
    container_name: yaze-certbot
    volumes:
      - ./nginx/ssl:/etc/letsencrypt
      - certbot_webroot:/var/www/certbot
    entrypoint: >
      /bin/sh -c "
        certbot certonly --webroot -w /var/www/certbot
        --email ercang@yazeproje.com
        --agree-tos --no-eff-email
        -d yazeproje.com -d www.yazeproje.com
        && trap exit TERM;
        while :; do certbot renew; sleep 12h & wait $${!}; done
      "
    depends_on:
      - nginx

volumes:
  postgres_data:
  redis_data:
  backend_uploads:
  frontend_node_modules:
  certbot_webroot:
